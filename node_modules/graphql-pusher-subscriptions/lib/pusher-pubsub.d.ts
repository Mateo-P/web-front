import { PubSubEngine } from 'graphql-subscriptions';
import * as Pusher from 'pusher';
export declare class PusherChannel implements PubSubEngine {
    private subPusher;
    private pubPusher;
    private channel;
    private channelName;
    constructor(options: Pusher.ClusterOptions & {
        channel: string;
    });
    publish(event: string, payload: any): Promise<void>;
    subscribe(eventName: string, onMessage: Function): Promise<number>;
    unsubscribe(): void;
    asyncIterator<T>(subjects: string | string[]): AsyncIterator<T>;
}
